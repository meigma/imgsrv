# Active toolchains for imgsrv.
# The current scaffold uses npm for the documentation site.

javascript:
  packageManager: 'npm'
  inferTasksFromScripts: true

npm: {}

node:
  version: '22.22.2'
  syncVersionManagerConfig: 'nvm'

# Add the Go toolchain here when the service module lands.
